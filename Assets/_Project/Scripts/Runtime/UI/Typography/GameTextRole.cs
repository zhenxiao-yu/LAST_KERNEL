namespace Markyu.LastKernel
{
    public enum GameTextRole
    {
        // MiSans Global — default for all readable UI text
        UI,

        // Sarasa Mono SC — machine/system voice: data feeds, readouts, code-like displays
        Terminal,

        // Oxanium — EN-only cyberpunk accent labels: phase names, speed buttons, HUD badges
        Accent,

        // Paid display font slot — logo, main title, brand moments
        Display,
    }
}
