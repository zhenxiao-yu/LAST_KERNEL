namespace Markyu.LastKernel
{
    public enum GameFontWeight
    {
        Regular,   // Body, labels, secondary info — default
        Medium,    // Buttons, list items, interactive text
        Semibold,  // Sub-headers, tabs, resource values
        Bold,      // Panel/modal titles, section headers
        Heavy,     // Screen titles (Victory/Defeat), logo
    }
}
