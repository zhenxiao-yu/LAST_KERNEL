# Scripts

New code should go into `Runtime` or `Editor`.

Recommended runtime domains:
- `Cards`
- `Board`
- `Systems`
- `Save`
- `UI`

The older placeholder folders are left in place so nothing breaks, but avoid putting new code there.
